enum buttonState{
	pressed,
	unpressed,
	hover
}


/*
	VARIABLE DEFINITIONS
	name - default - type (optional asset type)
	sprite - sDefaultButton - asset(sprite)
	roomTrigger - rActSelect - asset(room)
*/

state = buttonState.unpressed